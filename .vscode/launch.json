{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Tap",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${relativeFile}",
            "outputCapture": "std"
        }
    ]
}

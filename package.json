{
  "name": "gh-deploy",
  "version": "0.1.0",
  "description": "Deploy using GitHub web hooks and deployments",
  "main": "bin/run.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "standard --fix"
  },
  "keywords": [
    "github",
    "deployment",
    "webhook"
  ],
  "author": "Assaf Sapir",
  "license": "MIT",
  "devDependencies": {
    "standard": "^14.3.4"
  },
  "dependencies": {
    "fastify": "^3.3.0"
  }
}
